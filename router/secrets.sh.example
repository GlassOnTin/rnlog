# Router secrets — copy to secrets.sh and fill in your values
# secrets.sh is gitignored and must not be committed

NETWORK_PASSPHRASE="your-network-passphrase"
TRANSPORT_IDENTITY_B64=""   # base64-encoded transport identity (64 bytes)
LXMD_IDENTITY_B64=""        # base64-encoded lxmd identity (64 bytes)

# To extract identity keys from an existing install:
#   python3 -c "import base64; print(base64.b64encode(open('/root/.reticulum/storage/transport_identity','rb').read()).decode())"
#   python3 -c "import base64; print(base64.b64encode(open('/root/.lxmd/identity','rb').read()).decode())"
